# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MSIT - Notifier' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

pod 'SwiftSoup'

  # Pods for MSIT - Notifier

  target 'MSIT - NotifierTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MSIT - NotifierUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
